# ********RoostGPT********

# Test generated by RoostGPT for test artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=efd1faefc2
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_others.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"

scenarios:
  - name: "Non-success API response tests"
    flow:
      - log: "First, testing with a valid id but incorrect token"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "IncorrectToken"
          expect:
            - statusCode: 401
      - think: 1

      - log: "Next, testing with invalid id and correct token"
      - delete:
          url: "/authorizations/invalid-id"
          headers:
            Token: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 404
      - think: 1

      - log: "Lastly, testing with invalid id and incorrect token"
      - delete:
          url: "/authorizations/invalid-id"
          headers:
            Token: "IncorrectToken"
          expect:
            - statusCode: 401
      - think: 1
