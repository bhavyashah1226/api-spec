# ********RoostGPT********

# Test generated by RoostGPT for test artillery using AI Type Open AI and AI Model gpt-4
# 
# Test generated for /authorizations/{id}_delete for http method type DELETE in artillery framework
# RoostTestHash=efd1faefc2
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "authorizations_id_delete_success.csv"
    fields:
      - "id"
    skipHeader: true
    delimiter: "^|^"
  environments:
    prod:
      target: "{{ $processEnvironment.API_HOST }}"
scenarios:
  - name: "Remove Authorized Account"
    flow:
      - log: "Starting scenario for removing authorized account"
      - delete:
          url: "/authorizations/{{ id }}"
          headers:
            Token: "{{$processEnvironment.AUTH_TOKEN}}"
          expect:
            - statusCode: 200
            - hasHeader: "api-version"
      - think: 1
